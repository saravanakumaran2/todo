apiVersion: v1
kind: Namespace
metadata:
  name: backend
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  namespace: backend
  labels:
    app: todolist-app
    tier: backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: todolist-app
      tier: backend
  template:
    metadata:
      labels:
        app: todolist-app
        tier: backend
    spec:
      containers:
        - name: backend-container
          image: joselin721/backend:latest
          ports:
            - containerPort: 8081
          env:
            - name: MYSQL_HOST
              value: mysql.sql.svc.cluster.local
            - name: MYSQL_PORT
              value: "3306"
            - name: MYSQL_USER
              value: root
            - name: MYSQL_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: mysql-secret
                  key: mysql-root-pass
                  optional: true
---
apiVersion: v1
kind: Service
metadata:
  name: backend-svc
  namespace: backend
spec:
  type: ClusterIP
  ports:
    - port: 8081
      targetPort: 8081
  selector:
    app: todolist-app
    tier: backend
